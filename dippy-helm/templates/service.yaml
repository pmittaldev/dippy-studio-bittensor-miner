apiVersion: v1
kind: Service
metadata:
  name: {{ include "dippy-helm.fullname" . }}
  labels:
    {{- include "dippy-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dippy-helm.selectorLabels" . | nindent 4 }}
