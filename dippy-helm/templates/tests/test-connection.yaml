apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dippy-helm.fullname" . }}-test-connection"
  labels:
    {{- include "dippy-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dippy-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
